[Unit]
Description=Promtail for Loki
After=network.target

[Service]
Type=simple

User=loki

ExecStart=/usr/local/bin/loki-linux-amd64 -config.file "{{ loki_config_file }}"  
Restart=on-abort



[Install]
WantedBy=multi-user.target
