[Unit]
Description=Promtail for Loki
After=network.target

[Service]
Type=simple

User=promtail

ExecStart=/usr/local/bin/promtail-linux-amd64 -config.expand-env=true -config.file {{ promtail_config_file }}

Restart=on-abort



[Install]
WantedBy=multi-user.target
